#include "ruprep.h"
#include "rustd.h"
#include <iostream>  // всё ещё нужен для std

целое главная() {
    // Объявляем переменные
    целое число = 42;
    дробное пи = 3.14159;
    строка приветствие = "Привет!";
    
    // Вывод в консоль
    вывод << приветствие << конец_вывода;
    вывод << "Число: " << число << ", Пи: " << пи << конец_вывода;
    
    // Условия
    если (число > 40) {
        вывод << "Число больше 40" << конец_строки;
    } иначе {
        вывод << "Число меньше или равно 40" << конец_вывода;
    }
    
    // Циклы
    для (целое и = 0; и < 5; и++) {
        вывод << "Счёт: " << и << конец_вывода;
    }
    
    вернуть 0;
}